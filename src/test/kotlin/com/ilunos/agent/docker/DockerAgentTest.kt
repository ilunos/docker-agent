package com.ilunos.agent.docker

/*@MicronautTest
class DockerAgentTest(private val application: EmbeddedApplication<*>) : StringSpec({

    "test the server is running" {
        assert(application.isRunning)
    }
})*/
